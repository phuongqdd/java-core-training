server:
  port: 8080

spring:
  application:
    name: identity-service

  datasource:
    url: jdbc:mysql://localhost:3306/user_service
    username: root
    password: Dp17103919@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # create, create-drop, update, validate
    show-sql: true       # log c�u SQL
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka  # địa chỉ Eureka Server
    register-with-eureka: true   # đăng ký service với Eureka
    fetch-registry: true         # lấy thông tin service khác từ Eureka

  instance:
    hostname: localhost          # hostname để Eureka registry lưu

jwt:
  signerKey: "2uN816Tv+fUd+qUi96HnVPRhDipv+E5xygDoaDm2dAUEoPzAqmfdS1N4hRDL7fxk"
